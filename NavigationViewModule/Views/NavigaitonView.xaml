<UserControl x:Class="NavigationViewModule.Views.NavigaitonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             xmlns:local="clr-namespace:NavigationViewModule.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel 
            Background="White"
            Orientation="Horizontal"
            FlowDirection="RightToLeft">
            
            <Button Command="{Binding NewPieceCommand}"
                    Width="120"
                    Margin="5"
                    Content="Nowy"/>
            
        </StackPanel>
        
        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="ClothesListRegion"/>
        
    </Grid>
</UserControl>
